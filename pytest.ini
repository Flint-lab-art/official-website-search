[pytest]
# 无参数时默认跑 tests/
testpaths = tests
# 让 tests/ 能 import core（pytest.exe 不会自动把项目根加进 sys.path）
pythonpath = .
# 排除运行产物/虚拟环境/开发目录，避免误扫与变慢
norecursedirs = .git .venv venv build dist dev profile profile_mobile screenshots *.egg-info __pycache__
